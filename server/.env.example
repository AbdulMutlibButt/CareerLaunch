PORT=4000
HOST=127.0.0.1
NODE_ENV=development
APP_ORIGIN=http://127.0.0.1:3000
MONGODB_URI=mongodb://127.0.0.1:27017/careerlaunch
JWT_SECRET=replace-with-a-random-secret-of-at-least-32-characters
# Deploy this server folder as a separate Vercel Hobby project's Root Directory.
# Configure NODE_ENV, APP_ORIGIN, MONGODB_URI and JWT_SECRET in Project Settings.
# Vercel supplies its runtime flag; do not set it.
# Optional development-only value used when deliberately creating reusable demo logins.
DEMO_PASSWORD=
